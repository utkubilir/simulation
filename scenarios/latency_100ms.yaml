# Latency 100ms Scenario - Pipeline delay simulation
# Expected: Lock success with delayed response

name: latency_100ms
difficulty: hard
expected_detection: true
expected_success_locks_min: 1
duration: 60.0

world:
  size: [2000, 2000]
  safe_zone: [200, 200, 1800, 1800]

player:
  position: [500, 500, 100]
  heading: 45
  speed: 25.0

enemies:
  - id: target_1
    behavior: straight
    position: [800, 800, 100]
    heading: 225
    speed: 15.0  # Slow for easier tracking

camera:
  width: 640
  height: 480
  fov: 60

pipeline:
  detection_latency_ms: 100
  tracking_latency_ms: 50

lock:
  success_duration: 4.0
  margin_h: 0.5
  margin_v: 0.5
  min_confidence: 0.5

notes: >
  Simulates 100ms detection pipeline latency.
  Tests system behavior with delayed feedback.
